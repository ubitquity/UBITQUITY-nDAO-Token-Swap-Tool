import React from "react";
import ReactDOM from "react-dom/client";
import SwapTool from "./SwapTool.jsx";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SwapTool />
  </React.StrictMode>
);
